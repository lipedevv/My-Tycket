# 🚀 WhatTicket Plus - Sistema de Atendimento WhatsApp

### **Ubuntu Server 22.04 LTS**:

No comando, altere essa linha antes de começar:
https://github.com/SeuUserGit/SuaPasta.git
(demais informações, não alterar!)


```bash
sudo bash -c "apt update && apt upgrade -y && apt install sudo git -y && rm -rf whaticketplus && git clone https://github.com/SeuUserGit/SuaPasta.git whaticketplus && cd whaticketplus && chmod +x ./whaticketplus && ./whaticketplus"
```
